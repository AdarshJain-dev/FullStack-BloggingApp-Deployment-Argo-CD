apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: bloggingapp-gateway
  namespace: blogging
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - adarsh-jain-ecom.gt.tc
